# Daily Log — 2026-06-12

## Tasks Completed

### Promo Widget Redesign — `code fr.liquid`
- **What was built:** Redesigned product page coupon dropdown — replaced plain offer rows with coupon card rows showing visible code badge + COPY button.
- **File delivered:** `code fr.liquid`
- **Key changes:**
  - Added `.promo-card-left` column with offer text + dashed code badge (e.g. `LUMIERE10`)
  - Copy button min-height raised to 44px (WCAG touch target)
  - Mobile: rows wrap so copy button goes full-width beneath code badge
  - All existing JS reused unchanged (`promoToggle`, `promoCopy`, `copyFallback`)
- **Accessibility:** `aria-expanded`, `aria-controls`, `aria-label` on copy buttons, `role="list/listitem"`, Escape key support, `focus-visible` rings
- **Reusable:** Yes — pattern applies to any Shopify store with a discount code dropdown widget
- **Pattern name:** `coupon-card-row-widget`

---

## CAPABILITY LOG
- What was built: Coupon card row layout with visible code badge inside a dropdown widget
- Reusable: Yes
- If yes, where it applies: Any product page promo dropdown across LEDSone or other stores
- Pattern name: `coupon-card-row-widget`
